"""
Plotly chart builders for the Space-Health Dashboard.

Each function takes a clean DataFrame and returns a plotly.graph_objects.Figure
ready to drop into st.plotly_chart. Charts are styled for publication
(neutral palette, large font, clean axes).

Status: STUB — implement during Tab 1 (Overview) build.
"""

from __future__ import annotations

# TODO: Standard color palette per disease area
# TODO: donut_by_disease(df)
# TODO: bar_by_agency(df)
# TODO: timeline_by_year(df)
# TODO: treemap_disease_by_agency(df)
# TODO: sankey_translational_pipeline(experiments_df, trials_df, therapies_df)
# TODO: heatmap_disease_intensity(df)
# TODO: radar_disease_comparison(df)
